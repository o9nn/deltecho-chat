/**
 * EchoDream — Knowledge Integration System
 *
 * Implements the dream → interest → attention → discussion/practice → experience → dream
 * autonomy loop. DTE can wake and rest as desired, consolidating experiences into wisdom
 * during dream states and using dream insights to direct attention when awake.
 */

export {
  EchoDreamEngine,
  type DreamState,
  type EchoDreamConfig,
  type DreamExperience,
  type DreamInsight,
  type InterestPattern,
  type EchoDreamState,
  type EchoDreamEvent,
} from "./EchoDreamEngine.js";
